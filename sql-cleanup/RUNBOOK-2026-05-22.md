# Runbook de ejecución — Auditoría duplicados + facturas (2026-05-22)

Orden recomendado. Todo se ejecuta **manualmente** (DBeaver / deploy); nada automático.
La BD es **producción** (`34.70.18.38/toniclife_db_v2`). Ejecutar SQL dentro de `BEGIN; … COMMIT;`
y hacer `ROLLBACK` si algo no cuadra.

---

## ❌ NO ejecutar (ya aplicados en prod)
`01_dedup_access_logs` · `02_dedup_customer_addresses` · `03_dedup_product_exemptions` ·
`04_dedup_customer_social_profiles` — verificado el 2026-05-22 (ya corrieron). Re-correrlos no aporta.

---

## GRUPO A — Facturas (descarga CFDI on-demand)  ✅ recomendado, bajo riesgo

| # | Acción | Dónde | Nota |
|---|--------|-------|------|
| A1 | **Deploy del backend** con el cambio de `billing.service.ts` (`resolveFacturamaId`) | Railway | Hacerlo **primero**: deja la descarga on-demand funcionando antes de quitar los links. |
| A2 | Correr **`08_null_dead_invoice_xml_urls.sql`** | DBeaver | Pone en NULL las 5,832 URLs muertas (`tonic-life.net`). Crea su propio backup. Reversible. |
| A3 | **Validar**: abrir 1 factura migrada en admin V2 y descargar XML/PDF | Admin V2 | Confirma que Facturama aún conserva los CFDI viejos (2022-2023). Los que no → portal SAT. |

> Si la validación A3 falla masivamente (Facturama ya no tiene CFDIs antiguos), **revertir A2**
> con su bloque DOWN y reconsiderar (p.ej. recuperación batch desde SAT).

---

## GRUPO B — Limpieza + endurecimiento de duplicados BD  ⚠️ opcional (solo 13 filas)

Prerequisito: ninguno. `00` es read-only (verificación).

| # | Acción | Dónde | Nota |
|---|--------|-------|------|
| B0 | (Opcional) Correr **`00_detect_duplicates.sql`** | DBeaver | Read-only. Confirma estado: esperado `shopping_cart_items=11`, `purchase_order_cost_centers=2`, resto 0. |
| B1 | Correr **`05_dedup_shopping_cart_items.sql`** | DBeaver | **Revisar el paso 0** (las copias pueden diferir). `BEGIN…COMMIT`. Borra 11. |
| B2 | Correr **`06_dedup_purchase_order_cost_centers.sql`** | DBeaver | **Revisar el paso 0**: los montos pueden diferir; si difieren, NO borrar a ciegas. Borra 2. |
| B3 | Correr **`07_harden_unique.sql`** | DBeaver | **Solo después de B1+B2.** ⚠️ MLM (network_*_overrides) → confirmar. **Fuera de transacción** (usa `CREATE INDEX CONCURRENTLY`): ejecutar sentencia por sentencia, NO dentro de `BEGIN`. |
| B4 | (Opcional) Re-correr **`00`** | DBeaver | Confirmar que quedó todo en 0. |

> El Grupo B es independiente del A. Puedes hacer solo A ahora y dejar B para después.

---

## INDEPENDIENTE — arreglar el MCP de v2 (calidad de vida, no bloquea nada)
En `~/.claude.json`, server `postgres`: cambiar `?sslmode=require` → `?sslmode=no-verify`
y reiniciar Claude Code. (Permite consultas read-only a v2 vía MCP en futuras sesiones.)

---

## Resumen ultra-corto
- **Solo facturas:** A1 (deploy) → A2 (`08`) → A3 (validar).
- **Todo:** A1 → A2 → A3, y luego B1 (`05`) → B2 (`06`) → B3 (`07`, fuera de transacción).
- **Nunca:** re-correr `01`–`04`.
